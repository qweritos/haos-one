diff --git a/usr/src/supervisor/supervisor/__main__.py b/usr/src/supervisor/supervisor/__main__.py
index 686e83f09..d0731d1a5 100644
--- a/usr/src/supervisor/supervisor/__main__.py
+++ b/usr/src/supervisor/supervisor/__main__.py
@@ -22,7 +22,6 @@ _LOGGER: logging.Logger = logging.getLogger(__name__)
 
 CONTAINER_OS_STARTUP_CHECK = Path("/run/os/startup-marker")
 
-
 def run_os_startup_check_cleanup() -> None:
     """Cleanup OS startup check."""
     if not CONTAINER_OS_STARTUP_CHECK.exists():
@@ -37,6 +36,8 @@ def run_os_startup_check_cleanup() -> None:
 # pylint: disable=invalid-name
 if __name__ == "__main__":
     bootstrap.initialize_logging()
+    
+    _LOGGER.warning("Patched supervisor. for reference: https://github.com/qweritos/hass-supervised-docker")
 
     # Init async event loop
     loop = asyncio.new_event_loop()
